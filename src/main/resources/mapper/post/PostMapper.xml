<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vibeapp.post.PostRepository">

    <select id="findAll" resultType="com.example.vibeapp.post.Post">
        SELECT 
            NO as "no",
            TITLE as "title",
            CONTENT as "content",
            CREATED_AT as "createdAt",
            UPDATED_AT as "updatedAt",
            VIEWS as "views"
        FROM POSTS
        ORDER BY NO DESC
    </select>

    <select id="findAllWithPaging" resultType="com.example.vibeapp.post.Post">
        SELECT 
            NO as "no",
            TITLE as "title",
            CONTENT as "content",
            CREATED_AT as "createdAt",
            UPDATED_AT as "updatedAt",
            VIEWS as "views"
        FROM POSTS
        ORDER BY NO DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="incrementViews">
        UPDATE POSTS SET VIEWS = VIEWS + 1 WHERE NO = #{no}
    </update>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM POSTS
    </select>

    <select id="findById" resultType="com.example.vibeapp.post.Post">
        SELECT 
            NO as "no",
            TITLE as "title",
            CONTENT as "content",
            CREATED_AT as "createdAt",
            UPDATED_AT as "updatedAt",
            VIEWS as "views"
        FROM POSTS
        WHERE NO = #{no}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="no">
        INSERT INTO POSTS (TITLE, CONTENT, CREATED_AT, UPDATED_AT, VIEWS)
        VALUES (#{title}, #{content}, CURRENT_TIMESTAMP, NULL, 0)
    </insert>

    <delete id="deleteById">
        DELETE FROM POSTS WHERE NO = #{no}
    </delete>

</mapper>
